node_modules/
data/*.db*
data/*.db
.env
.admin.env
.DS_Store
npm-debug.log*

# Generated prompt snapshots (npm run prompt)
gemini-prompt.*.md

# Android / Gradle
android/.gradle/
android/build/
android/*/build/
android/local.properties
android/.kotlin/
android/probe/build/
android/data/build/
# Signed AAB/APK exported for Play Console upload — a build output, not source.
android/app/release/

# Firebase — names the project, has no reason to be public
android/app/google-services.json
.firebase/
.firebaserc

# The release-signing keystore path and its passwords. android/keystore.properties.example
# documents the shape; this file holds the real values and never leaves this machine.
android/keystore.properties

# IDE window/tab layout — per-machine, not project config. The rest of
# .idea/ (vcs.xml, misc.xml, etc.) stays tracked deliberately.
.idea/workspace.xml
android/.idea/workspace.xml
